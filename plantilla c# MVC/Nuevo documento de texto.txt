Obtener Socios 

Views/MailAccount.aspx // vista html*********

<body>
    <div class="row mt-4">
       <div class="col-lg-12">
            <div id="data"></div>
            </div>
        </div>
    </div>
    <script src="Script/MailAccount.js"></script>
</body>


protected void Page_Load(object sender, EventArgs e)  // code behind***********
        {
            string answer = "";

            try
            {
                string action = Request["action"];

                BrPartner brPartner = new BrPartner();
                if (action == "list")
                {
                    answer = brPartner.Get(1);
                }

                if (action == "filter")
                {
                    string value = Request["value"];
                    answer = brPartner.Filter(value);
                }

            }
            catch (Exception)
            {
                answer = "";
            }

            Response.Write(answer);
        }

